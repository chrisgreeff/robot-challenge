# robot-challenge

To run the app:
```
$ node app.js
```

To run the test suite (**Note: You'll need to run `npm install` or `yarn` first before running the tests**):
```
$ npm run test
```

Or if you have yarn

```
$ yarn run test
```
